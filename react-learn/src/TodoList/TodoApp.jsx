import { useImmerReducer } from "use-immer";


let initialTodo = [];

function todoReducer(notes, action) {
    if (action.type === "")
}

export function TodoApp() {
    const [todos, setTodos] = useImmerReducer()
}